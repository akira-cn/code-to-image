{
  "name": "code-to-image",
  "version": "0.2.0",
  "description": "Transform blocks of code to a highlighted jpeg base64 image.",
  "main": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd src && http-server -c-1"
  },
  "keywords": [
    "code",
    "image",
    "base64"
  ],
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "author": "akira-cn",
  "license": "Apache License 2.0"
}
